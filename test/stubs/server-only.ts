// Test stub for the `server-only` guard. The real package throws when imported
// outside a React Server Component build; under Vitest (plain Node) we alias it
// to this no-op so server modules can be unit/integration tested directly.
export {};
